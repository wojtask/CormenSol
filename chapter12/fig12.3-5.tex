\begin{tikzpicture}[
	level/.append style = {sibling distance=30mm/2^#1},
	every label/.style = {index node, label position=left, draw=none, fill=none},
	index node/.append style = {rectangle, draw=none, fill=none},
	outer/.append style = {node distance=10mm}
]

\node[outer] (pic a) {
\begin{tikzpicture}[
	every node/.style = {tree node},
	anchor = center
]
	\node[label=$y$] (left subtree root) {4}
		child {node[label=$x$] {3}}
		child {node {7}
			child {node {6}}
			child[missing]
		};

	\node[right=50mm of left subtree root] (right subtree root) {7}
		child {node {6}}
		child[missing];
            
    \draw[arrow, shorten >=10mm, shorten <=10mm, dashed, thick] (left subtree root.east) -- node[index node, above] {$\proc{Tree-Delete}(T,x)$} node[index node, below] {$\proc{Tree-Delete}(T,y)$} (right subtree root.west);
    
    \node[subpicture label, draw=none, fill=none, left=15mm of left subtree root] {(a)};
\end{tikzpicture}
};

\node[outer, below=of pic a.south west, anchor=north west] (pic b) {
\begin{tikzpicture}[
	every node/.style = {tree node},
	anchor = center
]
	\node[label=$y$] (left subtree root) {4}
		child {node[label=$x$] {3}}
		child {node {7}
			child {node {6}}
			child[missing]
		};

	\node[right=50mm of left subtree root] (right subtree root) {6}
		child[missing]
		child {node {7}};
		
    \draw[arrow, shorten >=10mm, shorten <=10mm, dashed, thick] (left subtree root.east) -- node[index node, above] {$\proc{Tree-Delete}(T,y)$} node[index node, below] {$\proc{Tree-Delete}(T,x)$} (right subtree root.west);
    
    \node[subpicture label, draw=none, fill=none, left=15mm of left subtree root] {(b)};
\end{tikzpicture}
};

\end{tikzpicture}
