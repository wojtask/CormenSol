\begin{tikzpicture}[
	level/.append style = {level distance=7mm, sibling distance=50mm/2^#1},
	every label/.style = {index node, label position=left, inner sep=1pt, draw=none, fill=none},
	subtree node/.style = {tree node, draw=none, fill=none},
	outer/.append style = {node distance=7mm}
]

\node[outer] (pic a) {
\begin{tikzpicture}[anchor=center]
	\node[tree node, blackened, label=$k+1$] (left subtree root) {$\boldsymbol{C}$}
    	child {node[tree node, label=$k+1$] {$A$}
        	child {node[subtree node] {$\alpha$}}
            child {node[tree node, label=$k+1$] {$B$}
            	child {node[subtree node] {$\beta$}}
                child {node[subtree node] {$\gamma$}}
            }
        }
        child {node[tree node, label=$k+1$] {$D$}
        	child {node[subtree node] {$\delta$}}
            child {node[subtree node] {$\varepsilon$}}
        };

    \node[right=50mm of left subtree root, tree node, label=$k+2$] (right subtree root) {$C$}
        child {node[tree node, blackened, label=$k+1$] {$\boldsymbol{A}$}
        	child {node[subtree node] {$\alpha$}}
            child {node[tree node, label=$k+1$] {$B$}
            	child {node[subtree node] {$\beta$}}
                child {node[subtree node] {$\gamma$}}
            }
        }
        child {node[tree node, blackened, label=$k+1$] {$\boldsymbol{D}$}
            child {node[subtree node] {$\delta$}}
            child {node[subtree node] {$\varepsilon$}}
        };
        
    \draw (left subtree root.north) -- +(0, 2mm);
    \draw (right subtree root.north) -- +(0, 2mm);
    \draw[arrow, shorten >=15mm, shorten <=15mm, dashed, thick] (left subtree root.east) -- (right subtree root.west);
        
    \node[subpicture label, left=25mm of left subtree root] {(a)};
\end{tikzpicture}
};

\node[outer, below=of pic a] (pic b) {
\begin{tikzpicture}[anchor=center]
	\node[tree node, blackened, label=$k+1$] (left subtree root) {$\boldsymbol{C}$}
    	child {node[tree node, label=$k+1$] {$B$}
        	child {node[tree node, label=$k+1$] {$A$}
            	child {node[subtree node] {$\alpha$}}
                child {node[subtree node] {$\beta$}}
            }
            child {node[subtree node] {$\gamma$}}
        }
        child {node[tree node, label=$k+1$] {$D$}
        	child {node[subtree node] {$\delta$}}
            child {node[subtree node] {$\varepsilon$}}
        };
            
    \node[right=50mm of left subtree root, tree node, label=$k+2$] (right subtree root) {$C$}
        child {node[tree node, blackened, label=$k+1$] {$\boldsymbol{B}$}
        	child {node[tree node, label=$k+1$] {$A$}
            	child {node[subtree node] {$\alpha$}}
                child {node[subtree node] {$\beta$}}
            }
        	child {node[subtree node] {$\gamma$}}
        }
        child {node[tree node, blackened, label=$k+1$] {$\boldsymbol{D}$}
            child {node[subtree node] {$\delta$}}
            child {node[subtree node] {$\varepsilon$}}
        };
        
    \draw (left subtree root.north) -- +(0, 2mm);
    \draw (right subtree root.north) -- +(0, 2mm);
    \draw[arrow, shorten >=15mm, shorten <=15mm, dashed, thick] (left subtree root.east) -- (right subtree root.west);
        
    \node[subpicture label, draw=none, fill=none, left=25mm of left subtree root] {(b)};
\end{tikzpicture}
};

\node[outer, below=of pic b] (pic c) {
\begin{tikzpicture}[anchor=center]
	\node[tree node, blackened, label=$k+1$] (left subtree root) {$\boldsymbol{C}$}
    	child {node[tree node, label=$k+1$] {$A$}
            child {node[subtree node] {$\alpha$}}
            child {node[tree node, label=$k+1$] {$B$}
                child {node[subtree node] {$\beta$}}
                child {node[subtree node] {$\gamma$}}
            }
        }
        child {node[subtree node] {$\delta$}};
            
    \node[right=43mm of left subtree root, tree node, blackened, label=$k+2$] (middle subtree root) {$\boldsymbol{C}$}
        child {node[tree node, label=$k+1$] {$B$}
            child {node[tree node, label=$k+1$] {$A$}
                child {node[subtree node] {$\alpha$}}
                child {node[subtree node] {$\beta$}}
            }
            child {node[subtree node] {$\gamma$}}
        }
        child {node[subtree node] {$\delta$}};
            
    \draw[arrow, shorten >=12mm, shorten <=12mm, dashed, thick] (left subtree root.east) -- (middle subtree root.west);
        
    \node[right=43mm of middle subtree root, tree node, blackened, label=$k+1$] (right subtree root) {$\boldsymbol{B}$}
        child {node[tree node, label=$k+1$] {$A$}
            child {node[subtree node] {$\alpha$}}
            child {node[subtree node] {$\beta$}}
        }
        child {node[tree node, label=$k+1$] {$C$}
            child {node[subtree node] {$\gamma$}}
            child {node[subtree node] {$\delta$}}
        };
        
    \draw (left subtree root.north) -- +(0, 2mm);
    \draw (middle subtree root.north) -- +(0, 2mm);
    \draw (right subtree root.north) -- +(0, 2mm);
    \draw[arrow, shorten >=12mm, shorten <=12mm, dashed, thick] (middle subtree root.east) -- (right subtree root.west);
        
    \node[subpicture label, draw=none, fill=none, left=20mm of left subtree root] {(c)};
\end{tikzpicture}
};

\end{tikzpicture}
